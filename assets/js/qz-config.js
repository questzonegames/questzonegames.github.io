// ===== Quest Zone — Supabase project config =====
//
// The publishable key below is MEANT to be public — it's the standard
// Supabase client key (formerly called "anon public"), safe to ship in a
// static site. It has no power on its own; every table it can touch is
// locked down by Row Level Security (see supabase/schema.sql), so the key
// alone can never read or write another player's data.
window.QZ_SUPABASE_URL = 'https://cbwlhrymbciihpkfjzmd.supabase.co';
window.QZ_SUPABASE_ANON_KEY = 'sb_publishable_lLYeMHWVYW8UKvLiuYCEOQ_Mc1aSDOD';
